Template for SharepointPlus

# Generating the doc

1. Install `jsdoc`: `npm install jsdoc`
2. Use the template from this folder: `node "node_modules\jsdoc\jsdoc.js" sharepointplus-x.x.js -t jsdoc_template/ -d docs`